#!/usr/bin/zsh
xinput | egrep 'slave\s+pointer' | grep -vi xtest | while read line; do
    devid="`echo ${line##*=}|cut -f1`"                                          
    xinput set-prop "libinput Natural Scrolling Enabled" 1
    xinput set-prop "libinput Click Method Enabled" 0, 1
done
